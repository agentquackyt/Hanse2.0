{
  "name": "thetool",
  "module": "index.ts",
  "type": "module",
  "private": true,
  "devDependencies": {
    "@types/bun": "^1.3.10"
  },
  "peerDependencies": {
    "typescript": "^5"
  },
  "scripts": {
    "dev": "bun run --hot dev.ts",
    "build": "rm -r docs && bun build index.html --outdir docs && cp -r assets docs/assets"
  }
}
